<UserControl
    x:Class="avantgarde.Joysticks.Joystick"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:avantgarde.Joysticks"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="200"
    d:DesignWidth="200"
    >

    
    <Canvas>
        <Image Source="/Assets/joystick.png" Width="200" Height="200"/>
        
        <Grid x:Name="JoystickUI" Loaded="JoystickUI_Loaded" Height="200" Width="200">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Image Grid.Row="{x:Bind joystickStateY, Mode=OneWay}" Grid.Column="{x:Bind joystickStateX, Mode=OneWay}" Source="/Assets/joystick_ball.png"/>

            <Canvas x:Name="UpKey" Grid.Row="0" Grid.Column="1" Visibility="Visible" Opacity="0.5">
                <Rectangle Fill="Transparent" Width="66" Height="66"/>
            </Canvas>
            <Canvas x:Name="DownKey" Grid.Row="2" Grid.Column="1" Visibility="Visible" Opacity="0.5">
                <Rectangle Fill="Transparent" Width="66" Height="66"/>
            </Canvas>
            <Canvas x:Name="LeftKey" Grid.Row="1" Grid.Column="0" Visibility="Visible" Opacity="0.5">
                <Rectangle Fill="Transparent" Width="66" Height="66"/>
            </Canvas>
            <Canvas x:Name="RightKey" Grid.Row="1" Grid.Column="2" Visibility="Visible" Opacity="0.5">
                <Rectangle Fill="Transparent" Width="66" Height="66"/>
            </Canvas>


        </Grid>
        

        <Image Source="Assets/joystick.png" Width="200" Height="200"/>

    </Canvas>

</UserControl>
